{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Feed",
  "description": "Defines the format of the GET /feed endpoint",
  "definitions": {
    "event": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "eventable_model": {
          "enum": ["dataset", "request", "collection", "datasource", "discussion"]
        },
        "eventable_id": {
          "type": "string"
        },
        "created_at": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "eventable_id",
        "eventable_model",
        "created_at"
      ]
    }
  },
  "type": "array",
  "items": {
    "$ref": "#/definitions/event"
  }
}
